servers=http://commodity,http://javanode-outcome\
{%  for n in range(num_datanodes) %}{% if n !=current_num %},http://javanode{{ n }}{% endif %}{%endfor %}
#  uit enviroment variable  DATABASE_URI halen
datapath=/data/features_{{ current_num }}.parquet
secretServer=http://commodity
server.port=80
server=data_{{ current_num }}